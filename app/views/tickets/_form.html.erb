<div class = "container" >
	<div class = "col-md-4 col-sm-6">
		<%= simple_form_for ([project,ticket]) do |f| %>
			<%= f.input :name %>
			<%= f.input :description %>
			<%= f.button :submit, class: "btn-primary" %>
		<% end %>
	</div>
</div>